Adding moodle Dockerfile with moodle user
Added new user moodle for running crons.
version 4.3.2-debian-11-r1

Difference from actual Dockerfile

```markdown 
apt-get install nano 
RUN groupadd -g 4200 kubernetes
RUN useradd moodle -u 4201 -g 4200 -m -s /bin/bash
RUN echo "moodle" >> etc/cron.d/cron.allow
RUN chown -R cronuser:kubernetes /opt/bitnami/ /var/spool/cron/
```

# Usage
```bash
export _tag=`date +%Y%m%d`
docker buildx build --platform linux/amd64 -t public.ecr.aws/d9k2s2v9/moodle:$_tag .
docker push public.ecr.aws/d9k2s2v9/moodle:$_tag
```